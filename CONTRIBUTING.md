# Contributing

Please keep pull requests focused, include tests when behavior changes, and avoid committing generated build output, credentials, production endpoints, or signing files.

Before opening a pull request, run the relevant Flutter analysis and tests from `apps/mobile-flutter/`, and run `scripts/audit-open-source.ps1` when a change touches configuration, networking, logging, or release tooling.
